{
  "frontend/src/App.tsx": "Main app component handling auto-toll logic and authentication; replaced `new Date()` / `formatDateKey` with `getTodayIST()` for the auto-toll date key and day-of-week derivation, ensuring the toll is added on the correct IST calendar date.",
  "frontend/src/components/AuthGate.tsx": "Authentication gate component that shows a login screen for unauthenticated users and renders children for authenticated users.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog prompting first-time users to enter their name; exports ProfileSetupModalProps interface with required open and onSave props to fix the App.tsx TypeScript error, with prevented outside clicks and responsive mobile layout.",
  "frontend/src/components/AppHeader.tsx": "Sticky header with app branding, optional sync status indicator, and user dropdown; accepts optional syncStatus/lastSyncTime/syncError props to remain compatible with LedgerPage's existing usage while rendering SyncStatusIndicator only when sync props are provided.",
  "frontend/src/components/EmptyState.tsx": "Reusable empty state component that displays an icon/illustration, title, description, and optional action button.",
  "frontend/src/components/ResponsiveTableShell.tsx": "Wrapper component that provides horizontal scrolling for tables on mobile devices using ScrollArea.",
  "frontend/src/features/ledger/LedgerPage.tsx": "Main ledger page with unified tab navigation and per-tab independent date range storage; replaced `applyMergedState` with `mergeRestoreFromBackup` for sync and `clearAllLedgerData` for logout reset, removing the dependency on the now-removed context property.",
  "frontend/src/features/ledger/TravellerManager.tsx": "Card component for adding, editing, renaming, and removing travellers with balance validation using the updated calculateTravellerBalance function signature that blocks deletion when traveller has outstanding due and shows error toast.",
  "frontend/src/features/ledger/DateRangePicker.tsx": "Date range selector with two calendar popovers that supports both controlled mode (via value/onChange props) and context-backed mode (using shared ledger state) for synchronized date range selection.",
  "frontend/src/features/ledger/DailyParticipationGrid.tsx": "Responsive daily participation grid with weekend toggles, per-date bulk checkboxes, and the \"Mark all travellers for today\" checkbox — now using `getTodayIST()` from dateRange.ts so the correct IST calendar date is selected instead of a UTC-based day-ahead date.",
  "frontend/src/features/ledger/SummaryPanel.tsx": "Card displaying per-traveller summaries with weekday/weekend trip breakdown, charges, payments, balances, and Due/Overpaid/Settled status badges with red/green color styling, using CashPaymentForm and OtherPendingAmountForm components that manage their own dialog state.",
  "frontend/src/features/ledger/ExportButton.tsx": "Button component that opens the export report dialog for filtered CSV/PDF/XLSX exports.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching and saving user profiles with proper actor dependency handling and custom loading state management.",
  "frontend/src/hooks/useLedgerLocalState.ts": "Custom hook managing ledger state with localStorage persistence; adds `updateOtherPending` function to allow editing existing OtherPending entries by id, alongside all existing CRUD operations.",
  "frontend/src/utils/dateRange.ts": "Utility functions for date range operations including a new `getTodayIST()` function that returns the current date string (YYYY-MM-DD) in Indian Standard Time (UTC+5:30) by hardcoding the +19800 second offset — never relying on device timezone — plus a `getTodayDateIST()` helper returning a Date object, and updated `getCurrentMonthRange`/`getCurrentWeekMondayToFriday` to use IST.",
  "frontend/src/utils/money.ts": "Currency formatting and parsing utilities for INR amounts with both formatCurrency and formatINR exports (formatINR is an alias for formatCurrency).",
  "frontend/src/utils/exportExcel.ts": "CSV export utility generating downloadable CSV with daily grid and summary section using updated weekend inclusion logic that counts saved weekend trips even when weekend checkboxes are unchecked.",
  "frontend/index.css": "Global styles with warm neutral/amber OKLCH color palette for both light and dark modes, avoiding blue/purple tones.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom fonts (Inter, JetBrains Mono), extended color tokens, and custom shadows for the finance/tracking theme.",
  "frontend/src/main.tsx": "Application entry point that sets up React Query, Internet Identity provider, and renders the App component with correct CSS import from src folder.",
  "frontend/src/index.css": "Global styles with warm neutral/amber OKLCH color palette for both light and dark modes, table styling, and Tailwind directives.",
  "frontend/src/features/ledger/LedgerStateContext.tsx": "Context provider exposing all ledger state and actions to child components; adds `updateOtherPending` to the context interface so consuming components can edit existing pending amount entries.",
  "frontend/src/features/ledger/RatePerTripControl.tsx": "Input control component for editing the per-trip rate with validation to prevent negative values and display formatted currency preview.",
  "frontend/src/features/ledger/CashPaymentForm.tsx": "Self-contained dialog component for adding a cash payment; replaced the local `getTodayStr()` UTC-based helper with `getTodayIST()` from dateRange.ts so the default date is always the correct IST date.",
  "frontend/src/features/ledger/PaymentHistoryView.tsx": "Payment history view with filterable table of cash payments; passes `travellerName` as a separate prop to EditCashPaymentDialog and uses the two-argument `onUpdatePayment` callback to match the updated dialog interface.",
  "frontend/src/features/ledger/PaymentHistoryDialog.tsx": "Dialog wrapper component that presents the PaymentHistoryView in a controlled modal dialog with open/onOpenChange props for programmatic triggering from the Carpool Menu.",
  "frontend/src/features/ledger/EditCashPaymentDialog.tsx": "Dialog for editing an existing cash payment; uses the original `CashPayment` type from useLedgerLocalState and accepts `travellerName` as a separate prop alongside the payment object, with the original `onUpdatePayment(id, updates)` two-argument callback.",
  "frontend/src/features/ledger/DeleteCashPaymentAlertDialog.tsx": "Confirmation dialog component using AlertDialog primitives with clear English confirmation copy that invokes the remove action only on confirm.",
  "frontend/src/features/ledger/OtherPendingAmountForm.tsx": "Self-contained dialog component for adding an other pending amount; replaced the local `getTodayStr()` UTC-based helper with `getTodayIST()` from dateRange.ts so the default date is always the correct IST date.",
  "frontend/src/features/ledger/CarExpensesPanel.tsx": "Card component for managing expense records with Auto Toll toggle; replaced `format(new Date(), 'yyyy-MM-dd')` with `getTodayIST()` from dateRange.ts for all default date values so expenses default to the correct IST date.",
  "frontend/src/features/ledger/ExpenseHistoryDialog.tsx": "Dialog wrapper component that presents the ExpenseHistoryView in a controlled modal dialog with \"Expense History\" title and description referring to expense records.",
  "frontend/src/features/ledger/ExpenseHistoryView.tsx": "Expense history view component displaying filterable table of expense records with IndianRupee icon, date, category, amount, note, and action columns (Edit/Delete), passing expense category and amount to DeleteCarExpenseAlertDialog and handling deletion via callback without parameters.",
  "frontend/src/features/ledger/EditCarExpenseDialog.tsx": "Dialog for editing an existing car expense; restores the original onUpdateExpense(id, updates) callback signature to match ExpenseHistoryView's usage, with responsive mobile layout and full-width touch-friendly buttons.",
  "frontend/src/features/ledger/DeleteCarExpenseAlertDialog.tsx": "Confirmation dialog component using AlertDialog primitives with clear English confirmation copy that invokes the remove car expense action only on confirm.",
  "frontend/src/features/ledger/OverallSummaryPanel.tsx": "Panel component displaying overall financial summary with IndianRupee icon for Income, Car icon for Expense, respecting weekend inclusion settings, plus month-wise breakdown table using dailyData-driven income calculations including coTravellerIncomes.",
  "frontend/src/utils/weekendInclusion.ts": "Utility functions for weekend day detection, editability checks, and a new calculation-specific inclusion rule that counts weekend dates if the relevant checkbox is enabled OR the date has saved trip data in dailyData.",
  "frontend/src/features/ledger/ExportReportDialog.tsx": "Dialog component for configuring report exports with its own independent date range selector (DateRangePicker + MonthYearRangeSelector), report type selection, format selection, and comprehensive filter controls that do not affect page tab date ranges.",
  "frontend/src/utils/exportReport.ts": "CSV export utility for generating filtered standard reports with updated traveller summary columns (Traveller Name, Trip Count, Total Amount, Total Payment, Balance, State), routing PDF exports to appropriate report generators based on report type, and applying filters consistently across all sections as read-only operations.",
  "frontend/package.json": "Package configuration with added dependencies for PDF (jspdf, jspdf-autotable) and Excel (xlsx) export functionality.",
  "frontend/src/utils/tripCalculations.ts": "Utility functions for calculating income from dailyData with updated logic to include weekend trips when either the weekend checkbox is enabled or the date has saved trip selections, ensuring previously saved weekend data remains counted.",
  "frontend/src/utils/ledgerBalances.ts": "Utility function to calculate per-traveller balance within a date range using the updated weekend inclusion logic, returning an object with totalTrips, totalCharge, totalPayments, and balance properties.",
  "frontend/src/features/ledger/ClearDataPanel.tsx": "Password-protected panel component requiring exact password \"7Days#Week\" to unlock, displaying five destructive clear action buttons (clear all, daily data, cash payments, other pending, car expenses) with AlertDialog confirmations and success toasts.",
  "frontend/src/features/ledger/BackupRestorePanel.tsx": "Panel component for exporting ledger data to JSON and restoring from backup with non-destructive merge, now working purely with local files while the sync system automatically handles backend storage.",
  "frontend/src/utils/backupRestore.ts": "Utility functions for backup export/import including JSON file generation, validation, BackupPayload type, and deterministic merge logic for all ledger data including travellers, payments, expenses, trip data (OR logic), settings, coTravellerIncomes, and perDayAutoTollSelection.",
  "frontend/src/hooks/useAppDataSync.ts": "Custom hook orchestrating multi-device sync with stabilized callbacks, 3-second polling, focus/visibility-triggered refresh, and debounced outbound saves; fixes the TypeScript error by including the required `otherPendingAmounts` field (preserved from remote data or defaulting to `[]`) when constructing the `AppData` object for `saveAppData`.",
  "frontend/src/components/SyncStatusIndicator.tsx": "Sync status component displaying current sync state (idle/syncing/synced/failed) with icons, readable status text, and time-ago indicator stacked vertically for authenticated users, visible on both mobile and desktop.",
  "frontend/src/features/ledger/MobileLedgerSidebarNav.tsx": "Mobile-only sliding sidebar navigation component with \"Carpool Menu\" title, displaying all nine ledger sections (including \"Expense Record\" and \"Trips & Payment\" labels) plus Payment History, Expense History, and Export action items with icons.",
  "frontend/src/features/ledger/CoTravellerIncomeDialog.tsx": "Dialog for adding/editing co-traveller income; replaced the local `getTodayStr()` UTC-based helper with `getTodayIST()` from dateRange.ts so the date field defaults to the correct IST date.",
  "frontend/src/hooks/useAutoTollSettings.ts": "Custom hook for persisting Auto Toll settings to localStorage; now imports and exposes `getTodayIST` via a `getTodayISTDate` helper so callers can compare dates in IST without duplicating timezone logic.",
  "frontend/src/features/ledger/TripHistoryPanel.tsx": "Trip history panel displaying traveller/co-traveller trip entries and a new flat \"Other Pending Amounts\" section filtered by date range; each pending row shows Name, Date, Amount, Comment with Edit and Delete action buttons wired to the new dialogs.",
  "frontend/src/features/ledger/PaymentSummaryPanel.tsx": "Panel component displaying per-traveller payment summaries (total charge, payments, balance, status) with a breakdown showing Cash Payments and Other Co-Travellers as separate line items, and overall totals (total payments received including both cash and co-traveller income, total payment due) for the active date range with color-coded status badges.",
  "frontend/src/utils/exportPdf.ts": "PDF/print export utility supporting multiple report types (Standard, Monthly, Profit & Loss, Income, Expense) with updated traveller summary table columns, A4-friendly layouts with robust page-break handling, and read-only report generation that respects traveller filter modes and opens print dialog for browser-native PDF saving.",
  "frontend/src/utils/accountingStatements.ts": "Utility module providing computation-only functions to derive standard accounting statement data structures (Monthly Report, Profit & Loss, Income Statement, Expense Statement) from existing ledger data without any persistence or sync interactions.",
  "frontend/src/features/ledger/TripsPaymentYearSelector.tsx": "Year selector component with dropdown displaying a range of years (5 years back to 2 years forward) that triggers a callback when the user selects a different year.",
  "frontend/src/features/ledger/OverallMonthYearSelector.tsx": "Month and Year dropdown selector component for the Overall tab that synchronizes with the shared date range, automatically updating when manual date changes occur and supporting full-month, full-year, and custom range states with explicit TypeScript typing for year options array.",
  "frontend/src/features/ledger/MonthYearRangeSelector.tsx": "Reusable controlled component with Month and Year dropdowns that sets full-month/full-year ranges and displays Custom state when the range doesn't match an exact month or year.",
  "frontend/src/features/ledger/MultiCategoryExpenseForm.tsx": "Dialog for adding 3 expenses at once (Toll, CNG BRD, CNG AHM); replaced the local `getTodayStr()` UTC-based helper with `getTodayIST()` from dateRange.ts so all three expense entries default to the correct IST date.",
  "frontend/src/features/ledger/EditTripHistoryTravellerDialog.tsx": "Dialog for editing a traveller trip entry; TripHistoryEntry interface now uses `dateKey` (for data operations) and `displayDate` (for display) as separate fields, matching the shape passed by TripHistoryPanel.",
  "frontend/src/features/ledger/EditOtherPendingAmountDialog.tsx": "New dialog component for editing an existing OtherPending entry; pre-fills Name (read-only), Amount, Date, and Comment fields and calls `updateOtherPending` from LedgerStateContext on save.",
  "frontend/src/features/ledger/DeleteOtherPendingAmountAlertDialog.tsx": "New AlertDialog confirmation component for deleting an OtherPending entry; shows the traveller name and amount in the confirmation message and calls `removeOtherPending` from LedgerStateContext on confirm."
}